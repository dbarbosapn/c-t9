# T9 implementation in C (with GUI)

Usage:
<br/>
<br/>

make - Same as doing `make compile && make run && make clean`

make compile - Compile the program

make run - Run the compiled program

make clean - Clean the binary files

make test - Run tests
